# Admin Backend Access Token (Optional)
# Must match admin/backend/.env's ADMIN_SECRET_KEY. The Vite dev proxy injects
# it as `X-Admin-Token` on proxied /api requests, keeping the secret out of the
# browser bundle. Leave unset when the backend leaves ADMIN_SECRET_KEY empty.
ADMIN_SECRET_KEY=
