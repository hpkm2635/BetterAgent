{
  "name": "betteragent-admin-ui",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "BetterAgent 后台管理控制台 (独立 Vue3 项目，不依赖 BetterAgent frontend/)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "vite": "^6.2.0"
  },
  "overrides": {
    "esbuild": "^0.25.0"
  }
}
