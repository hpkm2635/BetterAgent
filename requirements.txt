# BetterAgent Unified Dependencies (Python >= 3.12)

# NATS Message Bus
nats-py>=2.8.0

# Core Data Schemas & Validation
pydantic>=2.7.0,<2.11.0
pyyaml>=6.0
python-dotenv>=1.0.0

# AI Provider SDKs
google-genai>=0.1.0
anthropic>=0.25.0

# MCP client/server (presenter tools: services/cognitive/mcp/, services/mcp_ppt/, services/mcp_vscode/)
# Pinned below 2.0: mcp 2.0.0 renamed the server API (FastMCP -> MCPServer).
mcp>=1.2.0,<2.0.0

# Memory & Storage
redis>=5.0.0
qdrant-client>=1.9.0
sqlalchemy>=2.0.0

# Audio Processing & Network
numpy>=1.26.0,<2.2.0
aiohttp>=3.9.0

# STT (services/stt/funasr_client.py bridges an external FunASR streaming
# WebSocket server -- FunASR/torch itself deliberately not listed, see
# services/stt/requirements.txt)
websockets>=12.0

# Telegram Adapter Utilities
telethon>=1.34.0

# Companion Tools (services/companion/ -- FastAPI service on :8096)
fastapi>=0.110.0
uvicorn>=0.27.0
apscheduler>=3.10.0
requests>=2.31.0

# Development & Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
