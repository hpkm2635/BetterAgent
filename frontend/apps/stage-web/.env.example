# Copy to .env (or .env.local) and fill in.
#
# Must match the Go core's WEBGATEWAY_TOKEN (see the root project's
# .env.example). Without it, the WebGateway rejects the /ws handshake with
# a 401 and this app will loop reconnecting forever. See docs/SECURITY.md.
VITE_BETTERAGENT_WS_TOKEN=
